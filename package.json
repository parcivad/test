{
  "name": "xleonplayz-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Ein Monorepo mit vielen Apps in verschiedenen Sprachen — als Pruefstand fuer die Erkennung.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
